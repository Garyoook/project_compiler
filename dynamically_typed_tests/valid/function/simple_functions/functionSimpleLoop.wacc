# define and call a function with a simple loop

# Output:
# 10

# Program:

begin
  int f(int n) is
    i = 0;
    while i < n do
      i = i + 1
    done;
    return i 
  end
  x = call f(10) ;
  println x 
end
