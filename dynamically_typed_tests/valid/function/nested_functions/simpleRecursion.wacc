# a simple recursive function

# Output:
# #empty#

# Program:

begin
  int rec(int x) is
    if x == 0 
    then
      skip
    else
      y = call rec(x - 1)
    fi ;
    return 42  
  end

  x = 0 ;
  x = call rec(8)
end
